"""Per-strategy env-yaml templates.

Each ``<BootStrategyClassName>.yaml`` file in this package is a labgrid
env template parameterised with :class:`string.Template` ``${var}``
substitutions. See :mod:`adi_lg_plugins.hw_ci.render_env`.
"""
