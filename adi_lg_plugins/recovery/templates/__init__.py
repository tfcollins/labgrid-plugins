"""Packaged template files for the recovery initramfs.

This package only exists so :mod:`importlib.resources` can locate the
``init`` and ``udhcpc-default.script`` files via ``files("...templates")``.
"""
